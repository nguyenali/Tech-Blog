{{!-- Displays individual post's info  --}}
 <div class="card">
    <div class="card-header">
    <span>by {{user.username}} on
        {{format_date created_at}}</span>
       <h3><a href="/post/{{id}}"> {{title}}</a>
       </h3>
    </div>
    <div class="card-body">
      <p>{{post_content}}</p>
    </div>
  </div>